from fastapi import FastAPI

app = FastAPI()

# get request as we are fetching data
@app.get('/') # this means whenever user trying accessing root redirect user to this functionality 
def root(user : str):
    return{
        "message" : "Welcome {user} , This is FastAPI endpoint"
    } 
